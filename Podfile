# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target '休闲一刻' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for 休闲一刻
     pod "AFNetworking"
     pod "SDWebImage"
     pod “AMap3DMap”
     pod “AMapSearch”
     pod “AMapLocation”
     pod “FTIndicator”
     pod “AVOSCloud"  
pod “AMapNavi”
  target '休闲一刻Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target '休闲一刻UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
